# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov >= 5.0.0
respx >= 0.21.0

# Linting
ruff >= 0.6.0

# Pre-commit
pre-commit>=3.8.0
detect-secrets >= 1.5.0

# Dev utils
ipython >= 8.0.0
jupyter >= 1.0.0